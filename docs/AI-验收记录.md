# AI 联动预览验收记录 · 2026-09-19

本次交付为独立开发预览，不是完整 spec 的生产验收通过版本。

## 已执行

- 独立 arm64 / macOS 13 目标构建成功；现有 Clips / Senses 编译警告未在本功能中改写。
- `Tests/verify-ai.command`：5 组测试全部通过。覆盖解析、格式/错误码、身份隔离、阈值去重、周期确认、会话证据/中止、缓存、显式连接、单飞、断开取消、偏好恢复、演示隔离和排队事件再验证。
- 原桌宠 `Tests/verify.command`：29 条 PASS，退出码 0。
- `codesign --verify --strict 奶灰.app`：通过。
- `git diff --check`：通过。
- 原始工作目录 git status 保持干净；本次未修改原安装包、原偏好或素材。
- 原生预览窗口：曾实测打开“未连接”与“演示”面板，双平台余额/倒计时/日期可见、滚动内容正常。
- UI 试验发现暂停动作存在 Swift exclusivity 问题，修复为进入 inout 之前捕获布尔值；增加对原生 selector 的直接回归测试，执行两次暂停/恢复均通过。
- 审查修复：徽标点击穿透、长气泡裁切、过期提醒再验证、断开/换号清理未读、睡眠比较基线、凭证授权迟到回写、重复启动提前退出时空 panel 的崩溃。

## 未通过实机验证的部分

- 最后可见 UI 复测时设备锁屏，未完成最终设置面板的全部点按与气泡悬停测试。
- 未连接实际账号，因此没有对真实额度与原应用读数、权限拒绝/允许后的实际登录方式进行对照。演示数据不是读取成功的证据。
- 未实际发送 macOS 系统通知、播放提醒声音或触发用户真实任务事件。
- 未进行连续 10 分钟 CPU/内存/frame-time 性能验收；不能宣称达到 spec 全部性能指标。
- spec 中尚未交付的高级设置与适配路径，完整列于根目录 `AI预览说明.md`。

所有测试 fixtures 均为合成数据。没有自动连接、读取真实凭证、修改 Codex / Claude 登录状态、安装 hooks、启用 Codenotch 接口或发布版本。
