# 奶灰 AI 联动预览

这是独立开发预览包，Bundle ID 为 `local.naigrey.desktop-pet.ai-preview`。不会替换原来的奶灰，不使用原版偏好，也不会从正式更新源自动更新。

## 开始使用

双击“启动AI预览.command”，或打开本目录的“奶灰.app”，在右键 / 菜单栏选择“AI 额度与任务…”。

两个平台默认未连接。点击对应“连接本机账号”后才开始读额度；Claude 可能出现 macOS 钥匙串授权窗口。拒绝后不会在后台反复弹窗，可主动断开再连接重试。账号登录失效时回原应用登录，再点刷新。

原版奶灰可与预览同时运行，屏幕可能出现两只猫；这是为了不替换现有版本。测试完成可以从预览的菜单退出。

若想先看界面，先退出正在运行的预览，再双击“演示额度面板.command”。标题明确标“演示”，所有额度都是固定示例，不访问真实账号，不发送系统通知、不保存演示偏好。已运行的预览不会因再次打开而切换模式。

## 已实现

- Codex 文件登录和默认 Claude Code OAuth 登录的额度只读查询。
- 会话 / 每周 / 额外窗口分别展示，剩余百分比、重置倒计时、绝对日期和数据新鲜度。
- 剩余 20%、10%、0% 默认阈值，可修改；同周期去重，跨多个阈值只发最严重的一条。
- 保守重置确认；预计时间到达不自动填满额度。
- 平台独立静音、声音 / 系统通知开关、本轮结束 / 等待 / 恢复开关、暂停一小时和每日免打扰。
- 可靠会话记录触发提示；未知状态不冒充成功。活动和未读徽标有独立点击区域。
- 小猫安全待机时展示气泡，并可做现有轻微叫声口型；不会强切大动作。额度面板打开、拖动、睡眠或其他动作时等待安全时机。
- 原猫交互和动画资源保持不变，气泡增加了宽度限制和自动换行。
- 已解决排队旧提醒、睡眠事件重放、断开后迟到结果、未读标记清理和凭证授权取消竞态。

## 当前边界

- Claude 首版实现 OAuth 路径，尚未接入 Desktop 缓存及 CLI `/usage` 回退；只读默认账号。部分安装 / 登录方式可能不可用，会显示具体状态。
- Codex 当前支持文件登录；Keychain-only 和 API-key-only 方式不能提供订阅额度时明确提示。CODEX_HOME 被读取时，额度与会话使用同一路径。
- Claude 登录令牌轮换后会保守重建提醒基线，避免跨账号比较；轮换边界可能漏掉一次阈值提醒。
- Codex 的任务联动仅支持可读的本地生命周期记录；桌面托管任务如果没有这些记录，显示未检测到，不猜测完成。Claude Desktop 权限弹窗不在保证范围内。
- 长时间静默、缺少进程身份、开始事件已离开受限日志尾部时保守降为未知，可能漏报，不误报成功。
- 额外窗口目前只展示、不主动告警；逐窗口配置、3秒内多窗口合并、气泡悬停暂停、点击气泡定位应用不在本预览中。入口是独立 AI 徽标或菜单。
- 新敲键盘 / 招手 / 起卧过渡不包含在本次；当前轻动作使用现有待机部件，不声称大动作素材已经验收。
- 真实账户读数与原平台的对照、macOS 通知授权后的投递、10分钟性能指标尚未实机验收；未宣称生产发布通过。

## 验证与构建

```sh
./Tests/verify-ai.command
./Tests/verify.command
AI_PREVIEW=1 ./build.command
```

AI 测试使用合成数据，包括额度解析、错误码、凭证隔离、阈值与重置、账号切换、过期队列、会话存活和中止、文件缓存、显式连接、单飞、取消与偏好保存，不读取真实凭证或发送账户请求。

开发来源与规格见 `docs/AI-Spec.md`，第三方协议参考和许可证见 `THIRD_PARTY_NOTICES.md`。不包含安装、发布、重置券使用、购买额度或自动执行任务。
