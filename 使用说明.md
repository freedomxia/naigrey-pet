# 奶灰桌宠

双击 **奶灰.app** 即可启动。猫咪出现在桌面上，菜单栏会有猫咪图标；应用不占 Dock。

## 怎么和它玩

- **单击**：招手打招呼（视频片段）。
- **双击**：趴下睡觉；睡着时再双击叫醒。
- **拖动**：把它搬到喜欢的位置，会被提起来、松手落地。
- **鼠标在头上来回划**：撸猫，它会眯眼、耳朵放松，偶尔「呼噜呼噜…」。
- **鼠标在附近移动**：它会盯着看，眼珠先转、头跟着歪。
- **右键猫咪或点菜单栏图标**：
  - 摸摸 / 打招呼、让奶灰睡觉 / 叫醒、现在散步、伸个懒腰、打个哈欠
  - **丢个毛线球 / 收起毛线球**：猫会走过去拍球，球滚走再去追，玩累了就停下。毛线球可以用鼠标拖着甩出去，或点一下拨动。
  - **自动散步**：开关随机走动。
  - **跟着我的作息**：见下文，默认开启。
  - **猫咪大小**：迷你 / 小巧（默认）/ 标准 / 大只。
  - 把奶灰叫回来、退出奶灰。

## 跟着你的作息（默认开启）

- 你离开电脑一会儿（约 1.5 分钟），它打个哈欠；再久一点（约 4 分钟）就趴下睡觉。深夜会更快犯困。
- 你一回来（动鼠标或键盘），它醒来说「你回来啦～」。
- 锁屏时它睡觉，解锁时醒来。
- 连续用电脑快一小时，它伸个懒腰提醒你休息（每 25 分钟最多一次）。
- 每天各一次：早上伸懒腰说早安，中午提醒吃饭，傍晚说辛苦啦，深夜打哈欠提醒早点睡。
- 下午两三点和深夜更容易打哈欠。
- 你正在打字时，它安静陪着，不做走路、打盹这类大动作。

只读取「距离上次键盘鼠标操作过了多久」（系统公开接口，不需要辅助功能或输入监控权限），不记录你按了什么。

## 动画是怎么来的

- **平时坐着待机**：用原来的猫咪图拆成部位，由 GPU 实时变形（呼吸、眨眼、看鼠标、甩尾巴、抖耳朵、打哈欠、喵）。
- **招手、玩球、走路、伸懒腰、打哈欠、趴下、睡觉、醒来**：来自绿幕视频，抠像后切成带透明通道的片段，按脚掌位置和坐姿高度对齐到桌面上的猫；走路时窗口移动速度和视频里着地爪子的后移速度一致，脚不打滑。
- 视频片段缺失时会自动退回纯动画版本。

大小、位置、自动散步和作息开关会保存在本机。无网络服务、无账号、无额外运行依赖，不自动设置开机启动。可以把整个 .app 拖进「应用程序」目录。

## 源码与重新构建

- `Source/Sprites.swift`：图集加载、姿态读取、点击透明判断、各尺寸布局、窗口基类。
- `Source/Rig.swift` / `Source/Renderer.swift` / `Source/Motion.swift`：部位遮罩、Metal 着色器、猫的实时动作与行为。
- `Source/Clips.swift`：视频片段库与播放窗口（两层交替播放，切换不闪）。
- `Source/Ball.swift`：毛线球的物理和拖拽。
- `Source/main.swift`：窗口、交互、作息规则、玩球流程、菜单。
- `Assets/clips/`：8 个透明 HEVC 片段和 `clips.json`（裁剪、脚掌锚点、循环、走路速度、球的交接）。
- `build.command`：重新构建，需要 Xcode Command Line Tools；会把 `Assets/clips` 拷进 App。
- `Tools/clips/make_clips.py 透明母版.mov Assets/clips [片段名…]`：从抠好的 ProRes 4444 母版重新切片段（需要 ffmpeg、numpy）。
- `Tools/clipcheck.command 输出.png`：把每个片段首尾帧按 App 的算法放到画的猫旁边，检查大小和脚的位置。
- `Tools/preview.command 输出.mp4 [秒数]`：离屏渲染动画版四宫格预览（需要 ffmpeg）。
- 诊断用环境变量（直接运行 `奶灰.app/Contents/MacOS/Naigrey`）：`NAIGREY_DEMO=1` 按固定顺序演示动作，`NAIGREY_DEMO=ball` 演示玩球，`NAIGREY_SNAPSHOT=/路径/前缀` 保存 GPU 画面，`NAIGREY_CPULOG=1` 打印 CPU 占用。

此包为 Apple Silicon Mac 构建，最低系统 macOS 13。使用本机 ad-hoc 签名，未进行 Apple 开发者公证；复制到另一台 Mac 后可能需要通过系统「仍要打开」放行。
