# 本机验收记录

## 3.2.1（2026-09-18）：素材自己在漂色，加了逐帧校色

现象：用户反馈「有点变色」。

查证：先排除了两处——抠像对猫身内部颜色的影响（平均 0，最多 G 通道 −11）、画的猫与生成猫的整体色差（灰毛白毛都只差 2–6，<3%）。真正的问题是**生成素材自己在一段之内漂色**（以画的猫的灰毛 139/122/117 为基准）：

| 片段 | 时刻 | 灰毛 RGB | 偏离 |
| --- | --- | --- | --- |
| walk | 5.0s | 124,107,106 | 16（背上长出虎斑，整体偏暗） |
| wake | 全程 | 132,112,113 | 9–11（偏冷偏粉） |
| yarn | 4.0s | 144,112,117 | 11（偏红） |

修法：抠像时加一道校色——每帧量猫身中灰毛的平均色，跟待机那只猫的基准比，算出逐通道伽马把它拉回去（用伽马不用增益，白毛才不会过曝），再按 0.72/0.28 的比例逐帧平滑，钳在 0.78–1.28 之间。

过程中踩的坑：第一版用绿幕参考图当基准，它是没有透明通道的 RGB 图，我的判断走了 alpha 分支，把**绿色背景当成了灰毛**（绿幕的亮度 111 正好落在取样窗口里），结果整只猫被往绿里推——校色后偏离从 16 涨到 38。改成「没有真正透明通道就按绿幕判断」，并直接从图集里抠出待机猫作基准。

实测：全部七段素材的最大偏离 16 → **12**，多数片段 3–9（walk 中段 12、yarn 白毛 12 是模型真的改了毛色花纹，伽马只能拉回亮度）。

顺带修正：`wake` 的缩放校正原本按被污染的参考图算出 ×0.76，用干净基准重算是 ×0.84，末帧与画的猫的吻合度也从 0.43 升到 0.58——仍是十段里最弱的一段，建议重新生成。

`Tests/verify.command` 27 项通过，接缝重合度不受影响（校色不动剪影）。

## 3.2.0（2026-09-18）：整只桌宠换成同一只猫，接缝消失

现象：动作之间还是「跳」。3.1.0 加了起身/坐下的过渡也没解决。

原因（量出来的）：待机用的是原来那只画的猫，动作用的是绿幕视频里的猫，**这是体型不同的两只猫**。按脚掌对齐、按坐姿高度缩放之后，两者剪影只有 0.31–0.48 重合；允许任意缩放最多也只能到 0.48，说明差的是形状不是大小。用户提供的 12 组序列帧跟画的猫是同一只（0.91），但跟视频猫只有 0.42——拿它做过渡只会把跳挪个位置。

做法：用户用桌宠自己的坐姿图（`outputs/给AI的参考图-奶灰坐姿-绿幕.png`，从 `cats.png` 里抠出来放到纯绿背景上）做图生视频，生成七段绿幕原片，每段都从这张图开始、结束回到同样的坐姿。我重写了抠像和切片，把整套素材换掉。

| 片段 | 来源 | 时长 | 说明 |
| --- | --- | --- | --- |
| standUp / walk / sitDown | walk-10s | 5.42 / 0.71 / 3.17s | 一段素材切三段：起身→走→停下坐回。走路是原地踏步，步态周期 17 帧，地面速度 310px/秒（追爪子测的），循环接缝做了两帧交叠混合 |
| wave | wave-6s | 4.21s | |
| yawn | yawn-6s | 3.79s | |
| stretch | stretch-10s | 8.58s | |
| lieDown / sleep | sleep-10s | 4.25 / 4.17s | 按动作停下来的时刻自动切开，后半段正放倒放来回循环 |
| wake | wake-8s | 7.50s | 见下方问题 |
| play | yarn-8s | 7.42s | 粉球从右侧滚入被拍走；球的进出位置和速度自动追出来 |

接缝实测（按爪子对齐的全分辨率剪影重合度，和画的猫比）：

| 接缝 | 旧素材 | 新素材 |
| --- | --- | --- |
| 画的猫 → standUp | 0.45 | **0.97** |
| sitDown → 画的猫 | 0.45 | **0.99** |
| 画的猫 ↔ wave | — | **0.99 / 0.99** |
| 画的猫 ↔ yawn | 0.46 | **0.99 / 0.98** |
| 画的猫 ↔ stretch | 0.35 | **0.99 / 0.98** |
| 画的猫 ↔ play | 0.64 | **0.99 / 0.99** |
| 画的猫 → lieDown | 0.48 | **0.99** |
| wake → 画的猫 | 0.77 | **0.43（不合格，见下）** |

新增工具（都在仓库里）：
- `Tools/checkvideo.command 视频 参考图`：判断一段生成的绿幕视频能不能用（背景、出画、是否一镜到底、体型稳定、像不像参考图），不合格会说清重做时该强调哪句话。用旧的 Seedance 原片校准过：能认出「猫有 21/361 帧贴边」和「不是参考图那只猫（0.51）」，且不会把猫动得快误判成切镜头。
- `Tools/key.command 视频 输出.mov`：平整绿幕抠像。第一版在毛发边缘留了一圈绿描边（向外扩张 alpha 把背景拉进来了），已修。
- `Tools/clips/cut.py`：切片。会自动找步态循环点（按爪子对齐比较）、测地面速度、掐掉头尾静坐、按运动进度做缩放校正、追粉球。
- `Tools/joincheck.command`、`Tools/stylecheck.command`：接缝逐格比对、三种素材体型比对。

App 侧：片段新增 `moveFrom`/`moveTo` 两个字段，说明「这一段里第几秒到第几秒猫真的在往前走」，窗口只在那段时间跟着移动——起身的前 1.6 秒猫还在站起来，窗口不会滑行。姿势表简化：新素材里 play 和 stretch 都是坐着开始坐着结束，`getUp` 不再需要。

实测：demo 全流程正常（坐→起身→走→坐下→趴下→睡→醒），首帧 1–32 毫秒，CPU 4–12%，内存 245–272MiB；`Tests/verify.command` 27 项通过。片段总体积 16MB → 24MB。

**遗留问题：wake（醒来）不合格。** 那段素材结尾的坐姿比参考坐姿更高更瘦（归一大小后姿势相似度只有 0.43，后段每一帧都试过，最好也是 0.43），是素材本身的姿势不同，不是大小问题——已按最佳吻合做了 ×0.76 的缩放校正，但形状仍对不上。需要重新生成这一段，提示词要强调「结尾必须回到和参考图完全一致的坐姿：身体团起来、尾巴卷在身侧、不要挺直坐高」。其余九段可用。

验证边界：接缝是用离线剪影比对和真实素材帧验证的；本终端没有录屏权限，屏幕上的观感仍需你确认。走路循环接缝 0.85（生成素材本身的周期性限制），已用两帧混合软化，快速移动时是否可见需实际观察。

## 3.1.0（2026-09-18）：动作之间有起身、坐下，像一镜到底

现象：用户要「一镜到底的感觉，有起身去做动作的」。之前每个动作是直接切进去的——坐着的猫下一帧就在侧身走路，中间少了起身和转身。

做法：那段 30 秒绿幕原片本来就是一镜到底拍的（坐 → 招手 → 玩球 → 起身 → 走 → 伸懒腰 → 坐 → 打哈欠 → 趴下 → 睡 → 醒 → 坐），当初只切了动作本身，把过渡当废料跳过了。现在把过渡也切出来：

| 片段 | 来源 | 时长 | 作用 |
| --- | --- | --- | --- |
| `standUp` | 21.30–22.04 秒**倒放** | 0.79s | 坐 → 站。原片里猫从头到尾没有从坐姿站起来过，只有这段「站着转身坐下」，倒放即可用 |
| `sitDown` | 21.30–22.04 秒正放 | 0.79s | 站 → 坐，回到画的猫 |
| `getUp` | 14.20 秒 → 走路片段首帧（404 帧） | 2.67s | 玩完球从趴姿爬起来、转身，**结尾正好是走路片段的第一帧** |

配套加了姿势状态机：猫随时处于坐 / 站 / 蹲 / 趴之一（画的猫永远是坐着），每个片段登记「从什么姿势开始、结束在什么姿势」，要播的动作姿势对不上时自动先播过渡，最多两段。实测链路：

- 散步：坐 → standUp → walk → sitDown → 坐
- 伸懒腰：坐 → standUp → stretch（自带坐下收尾）→ 坐
- 玩球：坐 → standUp → walk（走过去）→ sitDown → play（拍球）→ getUp（爬起来）→ walk（再追）→ …

顺带修的：真球原本在「开始播放」后 0.12 秒收起，中间插了坐下的过渡之后会提前消失；改成等拍球片段真正上屏再收，实测在 `clip play on screen` 后 0.2 秒收起。

验证：
- 新工具 `Tools/joincheck.command`：把每个接缝上「前一段结尾」和「后一段开头」按 App 自己的锚点算法叠在一起，输出 `outputs/动作接缝检查.png`。第一版 `getUp` 切到 15.6 秒就停了（猫还在转身），和走路片段对不上，据此把它接到走路首帧，重合度肉眼几乎完全一致。
- 第 27 项测试：clips.json 里每个片段都登记了姿势；从任意姿势出发，到任意动作的起始姿势最多两段过渡且必然收敛；任意姿势都有回到「坐」的路；起身和坐下的锚点互为镜像（否则起身坐下会横向漂移）。
- demo 全流程与玩球 demo 各跑一遍，链路如上，CPU 约 3–14%，内存 275MiB（片段从 12MB 增至 16MB）。

一并评估但没有采用的素材：用户提供的 `猫咪序列帧/补全动作 2`（12 组 × 12 帧 PNG）。逐组量过相邻帧剪影重合度与地面线漂移：待机/招手/打哈欠 0.98–0.99（等于静图），走路 0.74（最低 0.51）、地面线漂移 33px，起身 0.73、猫高度在 177–345px 之间变。每帧是独立生成的图而非同一段运动的连续帧，连起来会沸腾；透明通道另有约 2% 杂点。其中「敲键盘」的姿势值得单独用绑定系统做成实时动画，已记录为后续项。

验证边界：
- `standUp` 是倒放，尾巴与毛发的运动方向也是反的，慢动作看会有一点不自然；正常速度下不明显。
- 侧身起手的片段（走路、伸懒腰）在换入的 0.08 秒里仍有轻微重影，这是 3.0.5 「先盖后撤」交接的固有代价。
- 本终端没有录屏权限，链路是靠日志和离线接缝图验证的，实际观感需要你看。

## 3.0.5（2026-09-18）：修「换动作就闪一下」

现象：用户反馈每换一个动作就闪一下，不自然。

原因：换动作时画的猫和视频片段是**交叉淡化**——画的猫从 100% 淡出、片段从 0% 淡入。但它们在两个不同的窗口里，屏幕上看到的是「片段盖在猫上、猫盖在桌面上」，桌面透出来的比例是 (1−猫)×(1−片段)。两边各 50% 的那一瞬间，**桌面从猫身上透出 25%**，猫整个变半透明，看起来就是闪了一下。淡化 0.2 秒，每次进出动作各闪一次。

（之前修的两次是别的问题：3.0.1 修的是循环片段接缝空帧，3.0.2 修的是片段首帧来不及解码的空档。这次是合成方式本身的问题，所以前两次没修到。）

改法：不再交叉淡化，改成「先盖上，再撤走」——任何时刻至少有一张图是实心的。
- 进动作：片段用 ease-out 在 0.12 秒内盖上来；画的猫先原样留着（0.08 秒），等片段基本盖住之后再用 0.06 秒撤走。
- 出动作：画的猫先在片段底下升起来（0.06 秒，被不透明的片段挡着，看不见），然后片段再用 ease-in 淡出 0.2 秒。

实测（`Tools/fadecheck.command`，按窗口服务器的合成方式离线还原每一帧）：桌面透出峰值从 **25% 降到 1.7%**（出动作 0.0%），见 `outputs/切换对比-打哈欠.png`、`outputs/切换对比-走路.png`。代价是换入时有约 0.08 秒的轻微重影（两张图叠着），侧身起手的走路、伸懒腰片段比较明显；相比「透明闪一下」，重影读起来像一次快速过渡。

新增工具与测试：
- `Tools/fadecheck.command 输出.png [片段]`：把交接过程按真实合成方式画成胶片条，参数直接取自代码里的常量，不会和实现走偏。
- 第 26 项测试：用 Core Animation 的真实缓动曲线逐毫秒扫一遍两段交接，断言桌面透出全程 < 2%，并顺带断言「交叉淡化确实会到 25%」，防止以后有人改回去。
- demo 全流程复跑：七个动作进出正常，首帧 1–36 毫秒。

验证边界：胶片条是离线还原的合成结果（用的是片段真实首帧、画的猫真实渲染、CA 真实缓动曲线），不是屏幕录制；本终端没有录屏权限。重影的观感需要你实际看一眼。

## 3.0.3 / 3.0.4（2026-09-18）：GitHub 在线更新

仓库：<https://github.com/freedomxia/naigrey-pet>（公开）。更新源是仓库里的 `updates/latest.json`，安装包发在 Releases。

做法：
- App 每天最多查一次（安静地查），发现新版本冒泡提示，菜单变成「换上新衣服 · 版本号」；点了才下载。手动「检查更新…」会弹对话框。
- 安全：安装包用 Ed25519 私钥签名（私钥在 `~/.naigrey/release-key`，不在仓库），公钥编译进 App，签名不符直接丢弃；另比对 SHA-256；装前校验解压出来的 bundle id、构建号与它自身的代码签名；构建号只增不减；旧版本备份到 `~/Library/Application Support/奶灰/backup/`。
- 更新源按多平台设计（`mac` / `windows` 两条），Windows 版做出来可以直接接入，客户端不用改。
- `Tools/release.command <版本> "说明"` 一条命令完成：改版本号 → 构建 → 跑测试 → 打包 → 签名 → 发 Release → 更新 feed → 推送。

端到端实测（真机，不是模拟）：
- 留了一份 3.0.2（build 6）的 App，发布 3.0.3（build 7）后直接运行它：日志依次是「发现新版本 3.0.3（build 7）」→「更新包校验通过 3.0.3 (15112074 bytes)」→「已装好 …/奶灰.app」；进程号从 1609 变成 1657（自己重启了），磁盘上的 Info.plist 变成 3.0.3 / build 7。
- 备份目录里留下了 `奶灰-3.0.2.app`，内容确认是被替换掉的那一版；替换用的暂存文件没有残留。
- 升级后的那份再查一次更新：没有再提示，也没有报错（已是最新）。

`Tests/verify.command` 25 项通过，新增 3 项：
- 更新源解析，以及「同版本号不提示、降级不提示、要求更高 macOS 的版本不提示、坏 JSON 报错」；
- 签名校验：正确签名通过；改了字节不通过；改了字节并重算哈希仍不通过（哈希救不了你，签名才算数）；别的密钥签的不通过；App 里带的公钥确实是 32 字节 Ed25519 公钥；更新源必须是 https。
- 替换流程：新包就位、旧包进备份、暂存文件清干净、含空格和中文以及引号的路径能正确传给 shell。

验证边界：
- 只在这台机器上验证过「从项目目录里的 App 自我更新」。装在 `/Applications` 的情况没实测；代码里会检查父目录可写，不可写时报错并给「打开下载页」。
- 「每天自动查一次」的计时按 24 小时判断，没有等真实一天，是用手动触发验证的同一条代码路径。
- 更新失败的对话框（网络断开、签名不符）没有在 UI 上实际点过；签名不符的判断由单元测试覆盖。
- 更新包未经 Apple 公证，仍然是 ad-hoc 签名；换机器首次打开还是要「仍要打开」。

## 3.0.2（2026-09-18）：修切换动作时顿一下

现象：用户反馈「它切的时候顿了一下」。

原因：每个片段都是第一次用到时才现场打开文件、解码首帧。实测第一次播打哈欠，触发后 172 毫秒才有画面，其间主线程另外被占用约 11 毫秒——猫已经藏起来，视频还没来，中间就是一个空档。

修复：
- 启动时预热：循环片段（走路、睡觉）提前解码成内存帧；其余片段各准备一个暂停在首帧的播放器，用掉后 1.5 秒补一个。
- 换尺寸后重新预热，并丢掉按旧尺寸解码的帧（帧是按像素尺寸缓存的，留着只占内存）。

预热本身踩到一个坑：`AVPlayer.preroll` 在播放器还没 ready 时调用会抛异常，异常从 `applicationDidFinishLaunching` 里一路抛出去，启动流程后半段（状态栏菜单、决策定时器）就没执行——窗口还在、画面却是静止的，且日志一行都不打。改为监听播放器状态，ready 之后再 preroll。

实测（同一台机器、demo 模式）：
| 片段 | 修复前首帧延迟 | 修复后 |
| --- | --- | --- |
| 打哈欠 | 172 ms | 26 ms |
| 趴下 | 约 30 ms | 13 ms |
| 睡觉（循环帧） | 约 35 ms | 1 ms |
| 醒来 | 约 30 ms | 19 ms |
| 招手 | 约 30 ms | 14 ms |
| 伸懒腰 | 约 30 ms | 14 ms |

触发动作时主线程开销从 11 毫秒降到 1–3 毫秒；`Tests/verify.command` 22 项全部通过。内存与 3.0.1 持平（约 270MiB，循环帧常驻）。

验证边界：本终端没有屏幕录制权限，以上是进程内计时，屏幕上是否还有可感知的停顿需要你实际看一眼。

## 3.0.1（2026-09-17）：修画面一闪一闪

现象：用户反馈桌宠一闪一闪。当时猫因为长时间没有键鼠操作，按作息规则睡着了，屏幕上是循环播放的睡觉片段。

两个原因：
- 系统的循环播放器（AVPlayerLooper）每循环一轮换新的播放项，切换瞬间没有画面。
- macOS 会暂停被完全遮挡窗口的显示刷新（诊断日志中 `occluded=true`、刷新时钟 0 次/秒）。动作片段在独立窗口播放，却依赖主窗口的时钟推进。

修复：
- 循环片段（走路、睡觉）改为解码进内存后逐帧循环，循环点精确；睡觉改为正放+倒放来回播，呼吸起伏首尾天然吻合。
- 片段窗口自带显示刷新时钟；主窗口增加备用定时器（仅在窗口可见而刷新时钟长时间未触发时补画）。
- 主窗口刷新上限从默认的 120Hz 锁到 60Hz。

实测：片段稳定 24–25 帧/秒（与素材一致）；走路时 CPU 约 8%，睡觉约 4%，醒来约 2%；内存峰值约 270MiB（循环片段解码后常驻约 20MiB）。

## 3.0.0（2026-09-17）：视频动作 + 跟着作息 + 毛线球

环境：Apple Silicon MacBook，1728×1117pt、2x、120Hz；Swift 6.3.2；ffmpeg、avconvert。

改动：
- 绿幕视频（Seedance，1280×720，24fps，30 秒）抠像：局部颜色模型求透明度并按背景反推边缘颜色（去掉暗绿描边），猫身内部强制不透明（眼睛高光），限制在猫附近且去掉地面以下（去掉倒影和阴影碎斑）。
- 切成 8 个片段：招手、玩球、走路（去漂移、单步循环）、伸懒腰、打哈欠、趴下、睡觉（循环）、醒来；透明 HEVC 共 12MB。
- 片段在独立透明窗口播放，按白色爪子中心与地面对齐、按坐姿高度缩放；两层交替播放，连续动作（趴下→睡觉→醒来）不闪。
- 行为：单击招手、双击睡/醒、散步、玩球（走过去→伏低→拍球片段→真球从视频球出画处接着滚→再追）、作息规则（离开犯困入睡、回来醒、锁屏睡、久坐提醒、早中晚深夜问候、打字时不打扰）。
- 待机仍是实时部位动画（看鼠标、撸猫、眨眼、呼吸等）；走路动画改为四条腿分层、四拍步态（无视频时使用）。

已完成：
- `Tests/verify.command` 22 项通过：新增片段库解析、片段锚点落在爪子上（含镜像）、毛线球物理、四条腿各自不打滑、腿层遮罩等。
- 抠像逐项检查：浅/深/蓝三种背景放大比对黑边修复前后；眼睛高光、胡须、毛线球、睡姿、走路地面。
- `Tools/clipcheck.command`：8 个片段首尾帧与画的猫并排与叠加，大小一致；招手、哈欠、醒来结尾与坐姿基本重合。打哈欠起点从 22.0 秒改到 22.25 秒（原起点还在转身）。
- 走路速度：在稳定后的连续帧上加网格人工核对，着地爪子每帧后移约 15px（自动匹配算法把不同爪子配对，读成了 5.6px，已弃用）。
- 真实 App 演示模式：打哈欠、走路、趴下→睡觉、醒来、招手、伸懒腰都在触发后 0.2 秒内上屏，无「未就绪」告警；玩球 40 秒循环 6 轮，球交接后滚开再被追。
- CPU（单核，进程自测）：播片段约 4–5%，视频走路约 9–12%；内存峰值约 226MiB。

验证边界：
- 本终端没有屏幕录制权限，看不到桌面合成后的真实画面；视频层是否透明依赖 Apple 文档说明（AVPlayerLayer + 32BGRA）与片段在 AVFoundation 中确认带 alpha，未截图确认。
- 作息规则按逻辑实现，未等真实一小时、跨午饭、跨深夜实测；锁屏通知未实测。
- 视频猫与画的猫毛色、光影略有差异，切换时 0.2 秒交叉淡化；伸懒腰、走路片段开头是侧身，从坐姿切入会有明显换姿势。
- 原视频部分帧耳尖碰到画面上沿，招手片段耳尖略被裁。
- 玩球片段里的球是视频自带的红球，真实毛线球是粉色；片段播放时真球隐藏。
- 演示时曾出现一次玩球被中断（疑似有人点到了猫），已修复中断后球不再出现的问题。

复现：`./Tests/verify.command`；`Tools/clipcheck.command out.png`；`NAIGREY_DEMO=1` / `NAIGREY_DEMO=ball` 运行 App 查看日志。

## 2.0.0（2026-09-17）：部位动画，接近真猫

环境：Apple Silicon MacBook，内建显示器 1728×1117pt、2x、120Hz；Swift 6.3.2。

改动：
- 猫咪由 GPU 着色器实时变形：尾巴（尾尖滞后于尾根）、尾尖抽动、头部歪转与平移、双耳、眼珠、瞳孔缩放、口鼻、下巴、四条腿（前后两组）、睡姿背部呼吸、招手爪子。
- 眨眼：先把睁眼向眼皮线挤压，再换成闭眼图，避免交叉淡化的「玻璃眼」。
- 打哈欠、喵：从哈欠图、招手图按鼻子位置对齐取出嘴部，从上唇向下逐步露出，配合下巴下沉、眯眼、耳朵后撇。
- 行为：鼠标移动引起注意（眼先动、头后跟、瞳孔放大、耳朵竖起、尾尖抽动），头顶来回划为抚摸（眯眼、耳朵放松、呼噜气泡），失去兴趣后可能叹气；随机扫视、微小眼动、连眨与慢眨、抖耳、甩尾、嗅；走路脚掌不打滑并定期停下张望；睡眠中耳朵、鼻子、爪子抽动。
- 帧率按需在 30/60 帧之间切换。

已完成：
- release 编译、ad-hoc 签名；`Tests/verify.command` 16 项通过：原有素材/布局/边界测试，四个姿势的部位遮罩都非空且取值 0–1、参数通道不越界，行走速度等于着地脚掌后移速度，各姿势参数有限，Metal 着色器实际编译并在离屏纹理画出坐姿猫。
- `Tools/preview.command` 用与 App 相同的代码渲染 18 秒四宫格视频，逐帧检查：跟随鼠标歪头、嗅、喵（露舌头）、被抚摸眯眼、打哈欠、走路迈腿、睡觉、招手摆爪。
- 实际运行 .app 的演示模式，并截取 GPU 实际提交给窗口的画面（每半秒一张，共 51 张）：坐、喵、哈欠、走、睡、招手依次正确显示。
- 进程自测 CPU（单核百分比，含渲染提交）：安静坐着约 4–6%，走路或有动作时约 6–10%。
- 正常启动后持续运行，无崩溃报告。

验证边界：
- 本终端没有屏幕录制权限，无法截取桌面合成后的窗口；检查依据是 GPU 输出画面和离屏视频。
- 鼠标跟随、抚摸的真实鼠标交互未做自动化实测（不想在你使用电脑时移动光标）；逻辑由离屏视频中的脚本化鼠标验证。
- 抠图边缘在浅色背景上有一圈浅色绒毛边；试过去白边，会出现深色描边、更假，因此保留原样。
- 侧身走路没有闭眼图，眨眼为眯眼；同侧两条腿在图里重叠，前后两组交替迈步。
- 内存峰值约 166 MiB（主要是纹理与启动时烘焙的遮罩）。
- app 为本机签名构建，未做 Apple 开发者公证。

复现自动检查：在交付目录执行 `./Tests/verify.command`。

## 1.1.0（2026-09-17）：流畅动画 + 缩小

环境：Apple Silicon MacBook，内建显示器 1728×1117pt、2x、120Hz；Swift 6.3.2。

改动：
- 渲染从「20Hz 定时器 + draw() 重绘，部分姿态降到 10/5Hz」改为 Core Animation 图层动画，由系统渲染服务按显示器刷新率合成。
- 走路移动窗口改为 CADisplayLink 每帧推进（macOS 14+；macOS 13 退回 60Hz 定时器），并按物理像素取整。
- 六个姿态统一缩放、共用地面线，按轮廓重合度计算水平偏移，切换时不跳位。
- 尺寸改为 72 / 100（默认）/ 130 / 170pt 坐姿高度；1.0 默认坐姿约 230pt，验收残留设置为 320。

已完成：
- release 编译、ad-hoc 签名，`Tests/verify.command` 全部通过：六姿态透明度、点击遮罩与像素一致、四档尺寸下所有姿态（含镜像）都在窗口内、共用地面线、气泡留白、预缩放图尺寸。
- 离屏渲染真实 PetView 的动画中间帧并逐张检查：待机呼吸、招手（摇晃+爱心+气泡弹出）、坐→走（下蹲弹起中换图）、走路转身、走→睡、睡觉 z 字、醒来伸懒腰、提起与落地回弹、打哈欠，深浅两种背景。
- 首版交叉淡化在坐↔走、坐↔睡时会出现约 150ms 的「两只猫」重影；已改为在下蹲最低点快速换图，重影缩短到约 60ms。
- 实际启动新版 .app 并持续运行，保存的位置在变化（自动散步在移动窗口），进程稳定。
- 旧版偏好迁移：删除旧尺寸与自动散步设置，保留位置并按新窗口宽度居中换算。

验证边界：
- 本终端没有屏幕录制权限，无法对实际运行的桌面窗口截图或录屏；「流畅」的检查依据是离屏渲染的中间帧和动画实现方式，而不是真实屏幕录制。
- 本环境无法读取进程 CPU/内存，1.1 未做功耗采样。
- 拖动、菜单切换尺寸未通过 UI 自动化实测；拖动代码路径与 1.0 相同，尺寸布局由测试覆盖。
- 走路仍是单张侧身图加跳步动作，不是逐帧迈腿。
- app 为本机签名构建，未做 Apple 开发者公证。

复现自动检查：在交付目录执行 `./Tests/verify.command`。

## 1.0.0（2026-09-17）

- release 优化编译，并通过 ad-hoc 签名完整性校验。
- 六姿态读取、透明通道与非空内容检查；按实际素材边界分割。
- 负坐标显示器、超出屏幕边界、有效位置保持和小屏边界计算测试。
- 实际启动 .app；右键菜单、招手与文字气泡、双击入睡/叫醒、切换大只、退出重启保留偏好。
- 一次瞬时采样：CPU 2.4%，RSS 约 101 MiB。
